function middle(arr) {
  return Math.floor((0 + arr.length - 1) / 2);
}

//it will return index value of the middle element of the array
let result = middle([0, 3, 4, 7, 9, 12]);
console.log(result);
